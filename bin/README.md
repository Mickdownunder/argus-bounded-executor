# Argus Binaries

Canonical entrypoints:
- `argus-research-run`
- `argus-delegate-atlas`

These entrypoints must keep stdout `KEY=VALUE` envelope compatibility for June while writing the
canonical JSON contract artifact under the run directory.
